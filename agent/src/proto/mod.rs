// Thor Firewall — Protobuf generated code placeholder
// سيُولَّد هذا الملف تلقائياً من proto files عبر tonic-build
//
// لتوليد الكود: cargo build (يُشغّل build.rs تلقائياً)

// TODO: Add proto files in agent/proto/ and build.rs

pub mod thor {
    // Placeholder until proto codegen is set up
    // tonic::include_proto!("thor");
}
