# Thor Firewall — ML Layer Requirements
# ─────────────────────────────────────────────────────────────────────────────
# eBPF/XDP Intelligence + MARL + GNN + LLM + UEBA + Online Learning
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Deep Learning ────────────────────────────────────────────────────────
torch>=2.3.0
torchvision>=0.27.0
torchaudio>=2.3.0

# ── PyTorch Geometric (GNN — GraphSAGE + GATv2 for network topology analysis)
# https://github.com/pyg-team/pytorch_geometric
torch-geometric>=2.5.3
torch-scatter>=2.1.2
torch-sparse>=0.6.18
torch-cluster>=1.6.3
torch-spline-conv>=1.2.2

# ── Ray RLlib (Distributed MARL Training — مستوحى من Ray project)
# https://github.com/ray-project/ray
ray[rllib,tune,data,serve]>=2.32.0
ray[default]>=2.32.0

# ── Stable-Baselines3 (RL Baseline — للبدء السريع وMAPPO comparison)
# https://github.com/DLR-RM/stable-baselines3
stable-baselines3>=2.3.2
sb3-contrib>=2.3.0

# ── BentoML (Model Serving — production inference server)
# https://github.com/bentoml/BentoML
bentoml>=1.3.4
openllm>=0.5.0

# ── vLLM client (LLM inference — connects to vLLM OpenAI-compatible API)
# https://github.com/vllm-project/vllm
# vllm>=0.5.0  # installed in separate GPU container; use openai client here
openai>=1.35.0        # OpenAI-compatible client for vLLM API
transformers>=4.42.0  # for tokenization + fallback
accelerate>=0.31.0

# ── MLflow (Experiment Tracking + Model Registry)
# https://github.com/mlflow/mlflow
mlflow>=2.14.0
mlflow-skinny>=2.14.0

# ── Tianshou (RL — fast PyTorch-native, for prototyping before Ray scale-out)
# https://github.com/thu-ml/tianshou
tianshou>=1.1.0

# ── Core ML ────────────────────────────────────────────────────────────────────
numpy>=1.26.0,<2.0
pandas>=2.2.0
scikit-learn>=1.5.0
scipy>=1.13.0
joblib>=1.4.0

# ── Graph Analysis ────────────────────────────────────────────────────────────
networkx>=3.3
# DGL (Deep Graph Library — fallback GNN framework)
# https://github.com/dmlc/dgl
dgl>=2.1.0

# ── Anomaly Detection (UEBA) ──────────────────────────────────────────────────
pyod>=1.1.3           # 40+ anomaly detection algorithms
alibi-detect>=0.12.0  # streaming anomaly detection + drift detection

# ── Hyperparameter Optimization ───────────────────────────────────────────────
optuna>=3.6.0
optuna-integration[mlflow]>=3.6.0

# ── Explainability ────────────────────────────────────────────────────────────
shap>=0.45.0
lime>=0.2.0.1
captum>=0.7.0         # PyTorch model explainability

# ── Data Pipeline ─────────────────────────────────────────────────────────────
pyarrow>=16.0.0
datasets>=2.20.0      # Hugging Face datasets for CICIDS2018
huggingface-hub>=0.23.0

# ── API + Serving ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
httpx>=0.27.0
grpcio>=1.64.0
grpcio-tools>=1.64.0
protobuf>=4.25.0

# ── Monitoring ────────────────────────────────────────────────────────────────
prometheus-client>=0.20.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation-fastapi>=0.45b0

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.32.0
redis[asyncio]>=5.0.0
clickhouse-driver>=0.2.9
psutil>=6.0.0
pynvml>=11.5.0
tqdm>=4.66.0
rich>=13.7.0
structlog>=24.2.0
